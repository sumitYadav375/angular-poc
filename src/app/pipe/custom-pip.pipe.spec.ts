import { CustomPipPipe } from './custom-pip.pipe';

describe('CustomPipPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomPipPipe();
    expect(pipe).toBeTruthy();
  });
});
